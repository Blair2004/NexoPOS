interface:
  display_name: "Create NexoPOS Module"
  short_description: "Build NexoPOS modules (including POS cart mastery)"
  default_prompt: "Use $create-nexopos-module to create or extend a NexoPOS module. For POS/cart work, follow references/pos-lifecycle.md."
