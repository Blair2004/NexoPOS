<ns-new-password token="{{ $token }}" :user="{{ $user }}">
    <div class="w-full flex items-center justify-center">
        <h3 class="font-hairline text-sm">{{ __( 'Loading...' ) }}</h3>
    </div>
</ns-new-password>